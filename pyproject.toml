[project]
name = "state-wise"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "BasharMithan", email = "basharmithan@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
stateWise = "stateWise:main"

[build-system]
requires = ["uv_build>=0.12.2,<0.13.0"]
build-backend = "uv_build"
